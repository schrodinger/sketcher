{
    "name": "@schrodinger/sketcher__tests",
    "private": true,
    "version": "0.0.0",
    "type": "module",
    "scripts": {
        "postinstall": "playwright install --with-deps",
        "test": "node --test html_api.node.js && playwright test",
        "test:html-api": "node --test html_api.node.js"
    },
    "devDependencies": {
        "@playwright/test": "^1.56.1"
    }
}
