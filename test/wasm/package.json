{
    "name": "@schrodinger/sketcher__tests",
    "private": true,
    "version": "0.0.0",
    "type": "module",
    "scripts": {
        "postinstall": "playwright install --with-deps",
        "test": "npm run test:unit && playwright test",
        "test:unit": "node --test unit/*.unit.mjs"
    },
    "devDependencies": {
        "@playwright/test": "^1.56.1"
    }
}
