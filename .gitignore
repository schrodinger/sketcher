# byte-compiled python files
*.pyc
*.pyo
# emacs backup files
*~
# vim backup files
.*.sw[a-p]
.swp
# patch leftovers
*.rej
*.orig
# general backup files
*.bak
# Windows build debris
*.user
# ctags
tags
TAGS
# Eclipse-Pydev files
.project
.pydevproject
.settings
# IntelliJ Idea, PyCharm files
.idea
# waf files
*waf-*
*waf3-*
# Mac os files
.DS_Store*
# protobuf compiled file
*.pb.go
# pytest files
.pytest_cache
# compilation database
compile_commands.json
.vscode/
.venv
ext_bld
# node dependencies for the sketcher npm package's React build
wasm/package/node_modules/

# Local and downloaded WASM artifacts. These can contain hundreds of thousands
# of generated files; keep them available for test runs without asking Git or
# VS Code to enumerate them as untracked project changes.
/build/
/build-[0-9]*/
/wasm-build/
/wasm-dev/
/wasm-fast/
/test-results/
